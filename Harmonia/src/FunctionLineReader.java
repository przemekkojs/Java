import java.util.Vector;

public class FunctionLineReader 
{	
	public Vector<Note> Output(String input)
	{
		Vector<Note> res = new Vector<Note>();
		
		for(int index = 0; index < input.length(); index++)
		{
			
		}
		
		return res;
	}
}